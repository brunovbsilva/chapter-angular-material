import { SelectUiFilterSearchPipe } from './select-ui-filter-search.pipe';

describe('SelectUiFilterSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new SelectUiFilterSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
